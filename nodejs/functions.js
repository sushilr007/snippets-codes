//Checking if it array or not
Array.isArray(response["json"]["ms_response"]["tasks"]["task"])


//It will check whether its "Not A Number"
Number.isNaN(bundle.inputData.project_id)

//It will remove "null,undefined" from array.
var _ = require('lodash');
_.compact(array);
